Flask web app which converts currencies by calling from an API.

Try to make the converter update as the user types, if the site sends a post request for the conversion rate as they select the currencies then hopefully it can be done client-side.

API used is @fawazahmed0's Free Currency Exchange Rates API with 200+ Currencies & No Rate Limits
https://github.com/fawazahmed0/exchange-api

Todo:
- Use Chart.js to add a graph showing historical exchange rates.